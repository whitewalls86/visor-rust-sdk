#!/usr/bin/env bash
set -e

echo "--- cargo fmt --check ---"
cargo fmt --check

# TODO: restore `--all-targets` once models and client methods are implemented (Phase 4+)
echo "--- cargo clippy ---"
cargo clippy --lib --all-features -- -D warnings

# TODO: restore `cargo test` once models and client methods are implemented (Phase 4+)
echo "--- cargo check --lib ---"
cargo check --lib

echo "All checks passed."
